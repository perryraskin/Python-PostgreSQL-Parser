# Python PostgreSQL Parser
Python program that parses web server access log file, loads the log to PostgreSQL and checks if a given IP makes more than a certain number of requests for the given duration.
